{"findings": [
{"step":"2.1a","severity":"blocker","category":"decision-20-superadmin","quote":"A super admin without an `active` membership ALSO gets `notFound()` here: super admins are `super_admins` rows, not org members (DESIGN §5), and they use `/admin`; add a code comment pointing at proposed step 3.11, which lifts this.","problem":"Orchestrator decision 20 settled the opposite: `requireOrgMember(slug)` admits an `active` membership OR a super admin whose entry has `membership_status = null` (with the 'Viewing as super admin' strip in 3.1). 3.1 already builds on the pass-through, so a 2.1a agent following this text produces a guard 3.1 must undo, and the founder cannot trial `/o/*` as super admin at Checkpoint B. Proposal 3.11 was rejected.","fix":"Rewrite the `require-org.ts` rule: `notFound()` unless the slug's entry has `membership_status = 'active'` OR (`isSuperAdmin` and `membership_status = null`); in the second case `membershipStatus = null`, `permissions = []` and every `requirePerm`/`hasPerm` check passes (mirrors `app.perm_org_ids`). Delete the 'proposed step 3.11' comment and the Finish/HANDOFF sentence 'super admins without an active membership get 404'. Keep `writable` and `org.org_id` as the contract."},
{"step":"3.1","severity":"major","category":"handoff-contract","quote":"`requireOrgMember(slug)` → `OrgContext = {org: {orgId, slug, name, status}, membershipStatus, personId, householdId, permissions: PermissionKey[], isSuperAdmin}`","problem":"2.1a fixes the contract as `OrgContext = { org: { org_id, slug, name, status }, …, isSuperAdmin, writable: boolean }` (and 3.3a/3.4a/3.5a cite `ctx.org.org_id` as 'the 2.1a contract'), while G7 (3.1, 3.2a, 3.2b) writes `org.orgId` and omits `writable`. Eight later prompts (3.5a, 5.1a, 5.3a, 5.4a, 5.5, 5.6b, 5.7b) carry a 'may be spelled orgId or org_id — read the file' hedge because of this.","fix":"In 3.1, 3.2a and 3.2b replace every `org.orgId`/`orgId` with `org.org_id` and list `writable` in the shape; 3.1 says 'extend 2.1a's contract, never reshape it'. Then delete the orgId/org_id hedge sentences in 3.5a, 5.1a, 5.3a, 5.4a, 5.5, 5.6b and 5.7b and state `ctx.org.org_id`."},
{"step":"2.1b","severity":"minor","category":"stale-proposal","quote":"accept an invitation to a church to open its admin area\" (DESIGN §5; proposed step 3.11 lifts this)","problem":"The `/o` placeholder copy tells super admins they need an invitation; decision 20 makes `/o/<slug>` open for super admins and 3.11 no longer exists.","fix":"Change the placeholder text to 'Super admins see every church here and can open its admin area (step 3.1)' and remove the 3.11 reference."},
{"step":"2.2b","severity":"minor","category":"decision-20-superadmin","quote":"under founder to-dos for step 2.5 add \"after creating the first church, invite yourself from the Admins tab (your account already exists from the sign-up, so 'Create account' would return…","problem":"Decision 20 removes the invite-yourself workaround (super admins pass through to `/o/*`).","fix":"Delete that founder to-do sentence; keep the `email_exists` mapping and the advice to use Invite for existing accounts in general."},
{"step":"2.5","severity":"major","category":"decision-20-superadmin","quote":"**Admins** tab → \"Invite\" → `<FOUNDER_EMAIL>`, all 12 permissions ticked → Create. Because Resend is not… … needed for the org pages of M3; super admins are not automatically members — see HANDOFF","problem":"Decision 20: 'G6 2.5's checklist stops inviting the founder into the first church'. The step still makes the founder invite himself and accept the link, and the goal/notes/done_when repeat it.","fix":"Remove the self-invite item, the goal sentence 'invite himself as that church's first admin' and the done_when clause; keep 'create a second test admin with Create account for a fresh email'. Add one line: 'the church's `/o/<slug>` pages open for you as super admin from step 3.1 on (decision 20)'."},
{"step":"3.7b","severity":"major","category":"stale-proposal","quote":"Guards: the rule for super admins on `/o/<slug>/*` is NOT settled — 2.1a's `requireOrgMember` returns `notFound()` for a super admin without an active membership (comment points at proposed 3.11), while 3.1 describes a \"Viewing as super admin\" strip","problem":"Decision 20 settled it (pass-through wins) and explicitly allows 3.7b to sign in as the super admin for `/o/*` checks; the prompt still treats it as open and only adds an optional test.","fix":"Replace the paragraph with: 'Super admins pass `requireOrgMember` with `membershipStatus = null` (decision 20). Add a required test: the super admin opens `/o/<tag>/users` and `/o/<tag>/settings`, sees the strip, and can save a setting.' Remove 'proposed 3.11' from Build and Finish."},
{"step":"2.3","severity":"major","category":"handoff-name","quote":"`packages/db/src/queries/orgs.ts` with `listOrganizationsForAdmin(client)`, which ALREADY joins the latest `billing_subscriptions` row","problem":"2.2a states the module is `packages/db/src/queries/organizations.ts` (NOT `orgs.ts` — 1.7a recorded the deviation) with `listOrganizations(db)` joining settings and the latest subscription. 2.3 names a file and function that do not exist (three places).","fix":"Replace every `queries/orgs.ts` / `listOrganizationsForAdmin(client)` in 2.3 with `packages/db/src/queries/organizations.ts` / `listOrganizations(db)`; keep 'read the file for the real signature'."},
{"step":"1.7a","severity":"major","category":"handoff-fact","quote":"`scripts/db/gen-types.sh` (runs `npx supabase@2.116.0 gen types typescript --db-url \"$DATABASE_URL\" --schema public > packages/db/src/database.types.ts`; the CLI connects directly and needs no Docker)","problem":"1.5 states the opposite: the CLI's `--db-url` path runs the postgres-meta Docker image, so 1.5's script runs postgres-meta directly under Node. 1.7a describes 1.5's output wrongly; an agent that 'fixes' the script to match this sentence breaks typegen on the Docker-less machine.","fix":"Describe the script as 1.5 built it: '`pnpm db:types` → `scripts/db/gen-types.sh` (postgres-meta under Node, no Docker, writes `packages/db/src/database.types.ts`); do not change how it generates.'"},
{"step":"1.7a","severity":"blocker","category":"missing-wrapper","quote":"`srvClaimNotifications(svc, { max_rows })` → `Tables<'notifications'>[]`","problem":"1.3b (decisions 15/21) ships `srv_claim_notifications(org_id uuid, max_rows int)`, `set_head_of_house(hh, new_head, previous_head_role)`, `srv_load_unlocked_share_link(link_id)`, `deactivate_account()`, `restore_my_account()`, `srv_archive_account(user_id, reason)` and `srv_restore_account(user_id)`. 1.7a wraps none of them and types the claim wrapper with the old parameter list, so 3.4b, 3.6a, 5.1a, 5.2b, 6.3a have nothing to call ('wrappers for every public RPC + srv_*').","fix":"Add to `rpc.ts`: `srvClaimNotifications(svc, { org_id, max_rows })`, `setHeadOfHouse(db, { hh, new_head, previous_head_role })`, `srvLoadUnlockedShareLink(svc, { link_id })` (parsed with `shareResolveResultSchema`), `deactivateAccount(db)`, `restoreMyAccount(db)`, `srvArchiveAccount(svc, { user_id, reason })`, `srvRestoreAccount(svc, { user_id })`; add the matching Zod inputs (`setHeadOfHouseSchema`, `archiveReasonSchema`) to 1.6b; extend the `09_grants`-parity type test."},
{"step":"1.4c","severity":"major","category":"stale-signature","quote":"`srv_claim_notifications(int)`, `srv_attach_user(uuid, uuid, uuid, text, permission_key[])` … `srv_claim_notifications(10)` → returns the queued rows","problem":"1.3b defines `srv_claim_notifications(org_id uuid, max_rows int)` (decision 21) and lists per-org/null-org cases; 1.4c's grants list and test 06 assert the old one-argument form, so `09_grants.sql`/`06_events_surveys.sql` fail against the file 1.3b writes.","fix":"Use `srv_claim_notifications(uuid, int)` in the section (9) list and test `srv_claim_notifications(<org A>, 10)` returns only A's due rows, `srv_claim_notifications(null, 10)` returns the rest, a second call returns nothing; `login(creator)` → 42501 unchanged."},
{"step":"1.4a","severity":"major","category":"decision-21-coverage","quote":"`account_deletion_requests`: select own.","problem":"Decisions 15/21 route the archival and correction schema into 1.2–1.4 'plus RLS tests'; G2 was revised but G3 was not. 1.4a's policy/grant sections do not mention: `ca_notifications_select` also for `bulletins.upload` where `kind = 'bulletin'` (G9 fix), `service_role` update/delete on `account_deletion_requests`, `profiles` archive columns, the `calendars.members_can_submit` and leader branch in `ca_events_insert` (G12 fix), or grants for `set_head_of_house`, `deactivate_account`, `restore_my_account`, `srv_load_unlocked_share_link`, `srv_archive_account`, `srv_restore_account`, `srv_claim_notifications(uuid,int)` in section (9)/`09_grants.sql`. Also 'see step 1.8 if the orchestrator adopted it' refers to a rejected step (same in 1.5 'proposed step 1.8').","fix":"Add those policies to the section (8) list and those signatures to the section (9) grant matrix and `09_grants.sql` (client RPCs to `authenticated`, `srv_*` to `service_role` only); state the widened `ca_notifications_select`. Replace the 1.8 sentences in 1.4a and 1.5 with '2.4 offers an optional throwaway-project dry run'."},
{"step":"1.4b","severity":"major","category":"decision-21-coverage","quote":"MISSING","problem":"No RLS test in 1.4b/1.4c covers the decision-15/21 objects 1.3a/1.3b now create: `app.guard_system_audience_groups`, `set_head_of_house` by a directory admin and by a household adult (and refusal of a plain `save_person` head change), an archived user (`profiles.archived_at`) reading nothing / absent from `srv_audience_user_ids` and `directory_persons`, `deactivate_account` + `restore_my_account` + `account_archived_by_admin`, `srv_load_unlocked_share_link` statuses, `calendars.members_can_submit = false` → member insert 42501, `on_content_publish` moving `scheduled_for`, `srv_process_account_deletions` respecting `scheduled_for`.","fix":"Add these assertions to 1.4b (`02_permissions`/`03_directory`/`04_sermons_share`) and 1.4c (`05`–`07`), each with `-- acceptance:` labels, and list them in the Verify counts; 1.3b's own Verify items are the specification."},
{"step":"3.6a","severity":"blocker","category":"stale-signature","quote":"`srv_claim_notifications(max_rows)` claims `queued` rows … it has NO org parameter — claiming is global across churches by design (proposed step 3.12 adds an `org` argument)","problem":"1.3b ships `srv_claim_notifications(org_id, max_rows)` (decision 21; 3.12 rejected). The dispatcher, `afterContentPublish({ orgId })`, the Loop text and the notes ('`dispatchQueued` has no org option') are built against the old signature, so the synchronous after-publish dispatch would claim other churches' rows and the typed wrapper would not compile. The numbering aside '9998 reserved for join codes, 9997 for surveys' also omits 9996.","fix":"`dispatchQueued(svc, { orgId, transport, limit, now, deadlineMs })` → `srvClaimNotifications(svc, { org_id: orgId, max_rows })`; `afterContentPublish({ orgId })` passes the org; 3.6b's cron passes `org_id: null`. Delete every 'proposed 3.12' sentence. Since 1.3a's `on_content_publish` now moves `scheduled_for`, keep the `future` branch only as a guard (no `attempts - 1` trick). Numbering note: 'next free number from 9995 downward; 9998/9997/9996 are reserved'."},
{"step":"6.2b","severity":"minor","category":"stale-signature","quote":"`srv_claim_notifications(max_rows)` claims `queued` rows with `scheduled_for <= now()` … which is why a moved `publish_at` needs the outbox row moved too.","problem":"Signature is `(org_id, max_rows)`, and 1.3a's `app.on_content_publish` already moves `scheduled_for` when `publish_at` changes, so the step's premise ('3.5a/3.6a left a queued push at the OLD scheduled_for') is false; a TypeScript move through the service client duplicates the trigger.","fix":"State the real signature and trigger behaviour; make Reschedule/Publish now update `announcements.publish_at` only and verify (Vitest + acceptance) that the outbox row followed; keep the status pages and Retry."},
{"step":"3.5a","severity":"minor","category":"stale-fact","quote":"Changing `publish_at` of an already-queued push does NOT move the queued `scheduled_for` in this step; show the notice \"Push already queued — the dispatcher checks the publish time before send…","problem":"After decision 21, 1.3a's trigger moves `scheduled_for` with `publish_at`; the notice and the 3.6a follow-up describe the old behaviour.","fix":"Replace with 'Push rescheduled to <new time> (the DB trigger moved the queued notification)'; add an action test asserting the notice text; drop 'does NOT move'."},
{"step":"3.4b","severity":"blocker","category":"stale-proposal","quote":"cookie-verified visits re-validated in TypeScript via `loadUnlockedShare` + the additive `getShareLinkForCookieVisit` query (candidate `srv_load_share_link` for a later migration — proposed step 3.11)","problem":"Decision 21 folded this into 9999: 1.3b ships `srv_load_unlocked_share_link(link_id uuid) returns jsonb` (same status vocabulary, 'so 3.4b stops re-validating in TypeScript' and 'so 3.4 can key the cookie by link_id'). 3.4b still builds the TypeScript duplicate and a service-client base-table query, and records it as debt for a rejected step.","fix":"`loadUnlockedShare` → one call to 1.7a's `srvLoadUnlockedShareLink(svc, { link_id })`; key the unlock cookie by `link_id` (`share_<linkId>` per DESIGN §7) with the JWT carrying `link_id`; delete `getShareLinkForCookieVisit` and every 'proposed step 3.11' sentence; update the 3.8 checklist sentence to name `srv_load_unlocked_share_link`."},
{"step":"3.8","severity":"minor","category":"stale-name","quote":"share page mints signed URLs only after `srv_resolve_share_link`/`loadUnlockedShare` succeeded","problem":"With 3.4b calling the RPC, the checklist item should name the two RPCs so the security review can grep the service-client whitelist.","fix":"Write '`srv_resolve_share_link` / `srv_load_unlocked_share_link`'."},
{"step":"5.1a","severity":"blocker","category":"stale-proposal","quote":"head-of-house is two `save_person` calls pending the atomic `set_head_of_house` RPC (proposed step 5.8) … leave a `TODO(5.8): replace with the atomic set_head_of_house RPC (proposed follow-up migration)` comment","problem":"Decision 21/30: `set_head_of_house(hh, new_head, previous_head_role)` is in 9999 (1.3b), `guard_persons_columns` stays strict, and 'the only way to move the head is the atomic RPC'. 5.8 is now the join landing page. The two-call demote/promote path is dead code that also fails under the partial unique index race the RPC exists to avoid.","fix":"`setHeadOfHouseAction(orgSlug, householdId, personId, previousHeadNewRole)` → single `setHeadOfHouse(db, { hh, new_head, previous_head_role })` (1.7a); map `household_role_not_allowed`/`not_household_editor`; delete the restore-on-failure logic, the TODO and 'proposed step 5.8'; `docs/RLS.md`: 'head-of-house changes go through `set_head_of_house`'."},
{"step":"5.2b","severity":"blocker","category":"stale-proposal","quote":"through an atomic `set_head_of_house` RPC) is proposed step 5.8 — leave a `TODO(5.8): replace with the atomic set_head_of_house RPC (proposed follow-up migration)` comment","problem":"Same as 5.1a: the mobile head-of-house picker mirrors 'the two-call head-of-house sequence in `actions.ts`', but a household adult's `save_person({ household_role: 'head' })` is refused by 1.3a's strict guard (decision 30); the feature would not work for members at all.","fix":"Call `setHeadOfHouse(supabase, { hh, new_head, previous_head_role })`; describe 1.3b's rule (admitted via `app.can_edit_household`, runs under `app.trusted_rpc`); remove the TODO, 'proposed step 5.8' and the notes sentence 'recorded for the proposed 5.8 migration'."},
{"step":"5.3a","severity":"blocker","category":"stale-proposal","quote":"record in HANDOFF \"Deviations from plan\" that `events_require_approval` is informational until a migration teaches the trigger to honour it (open issue for 5.6's `9998_*` migration)","problem":"Decision 21 folded the rejected 5.8 into 9999: 1.3a's `app.force_event_pending` honours `events_require_approval` and `calendars.members_can_submit`, and `ca_events_insert` has the leader branch. 5.3a still 'verifies' the old behaviour, points at 5.6's migration (which is join codes only) and never exposes `calendars.members_can_submit` — the UI parts of the rejected 5.8 prompt (schema field, column lists, form switch) now have no home.","fix":"State the 1.3a facts; add `members_can_submit: z.boolean().default(true)` to `calendarSchema` (additively), include the column in `listCalendars`/`getCalendar`, add the switch 'Members can suggest events on this calendar' to the calendar form with helper text, and let leaders (`announcements.create`) create approved events directly. Delete 'proposed step 5.8', 'leader branch until a migration' and the HANDOFF deviation sentence."},
{"step":"5.3b","severity":"major","category":"stale-fact","quote":"insert = member ∧ (`members_can_submit_events` — 1.4 says it added `∨ announcements.create` here; DESIGN §6 lists only the setting) … with `events_require_approval = false` assert whatever `app.force_event_pending` does","problem":"The behaviour is fixed by 1.3a/1.4a (decision 21), not to be discovered: `events_require_approval = false` → member insert stored `approved` with `push = false`; `calendars.members_can_submit = false` → member insert 42501; leaders insert via the `ca_events_insert` leader branch. Tests that 'assert whatever happens' pin nothing.","fix":"Assert those three outcomes in the RLS additions and hide calendars with `members_can_submit = false` from the mobile submit picker; drop 'the 5.3a HANDOFF findings' as the source."},
{"step":"5.5","severity":"major","category":"stale-fact","quote":"Nothing in the schema stops a manager from renaming or deleting a **system** row — the UI and Server Actions must refuse it (Build 4).","problem":"1.3a now ships `app.guard_system_audience_groups` (before update or delete on `audience_groups`, decision 21); the statement is false and the step's tests would expect a silent success path that no longer exists.","fix":"State the trigger and its error key (read 1.3a's catalogue); the UI still hides rename/delete on system rows, the Server Action maps the key, and a Vitest + the 1.4b RLS test assert the refusal."},
{"step":"6.1a","severity":"minor","category":"stale-proposal","quote":"if adopted, the atomic `set_head_of_house` RPC of proposed step 5.8 … 9997 was reserved for surveys by 3.5b/3.6a/5.8; if the ledger shows 9997 taken, use the next free lower number","problem":"The RPC exists in 9999 (1.3b); 5.8 is the join landing page; decision 21 reserves 9997 for this step with no contingency.","fix":"Write 'the atomic `set_head_of_house` RPC (1.3b); designation UI 5.1a/5.2b' and 'the file is `9997_surveys_head_of_house.sql` (reserved by decision 21)'."},
{"step":"6.1c","severity":"minor","category":"stale-proposal","quote":"which itself depends on proposed step 5.8 (atomic `set_head_of_house`) for household adults — if 5.8 was not adopted","problem":"Stale conditional in notes; the RPC is in 9999.","fix":"Replace with 'household adults move the head through `set_head_of_house` (1.3b, UI 5.2b)'."},
{"step":"5.7a","severity":"major","category":"renumbered-step","quote":"`/join` is added by the proposed step 5.9 … HANDOFF decision \"`/invite/*` is the only claimed path in Phase 2; `/join/*` waits for proposed 5.9\"","problem":"Decision 24 renumbered the join landing page to **5.8** and made **5.9** the FOUNDER Checkpoint D. 5.7a would write '5.9' into `app.config.ts` comments, HANDOFF and the session log, and 5.8's Inputs quote that HANDOFF sentence.","fix":"Replace every 'proposed 5.9' in 5.7a (Build, Constraints, Finish, notes) with 'step 5.8'; HANDOFF sentence: '`/join/*` is claimed in step 5.8'. Update the quoted sentence in 5.8's Inputs."},
{"step":"5.7c","severity":"minor","category":"renumbered-step","quote":"`/join/<code>` is NOT asserted (proposed 5.9) … Checkpoint D (Phase 2 trial) is proposed as a founder step 5.10","problem":"5.9 → 5.8 (join landing page), 5.10 → 5.9 (Checkpoint D).","fix":"Replace the ids in Build, Constraints and notes."},
{"step":"2.4","severity":"major","category":"stale-proposal","quote":"If `docs/TODO.md` contains a step 1.8 (throwaway-project dry run of `9999_init.sql`), do it first","problem":"Decision 24 rejected 1.8 and folded it into 2.4 as an optional dry-run item; the founder would search for a step that does not exist.","fix":"Replace with an optional item 0: 'Dry run (recommended, 15 min): create a throwaway Free project, paste `9999_init.sql` twice, paste `supabase/tests/rls/01_tenancy.sql` verbatim, then delete the project.' Remove the 1.8 sentence from the notes."},
{"step":"2.4","severity":"minor","category":"handoff-founder","quote":"Magic Link template includes `{{ .Token }}` (Confirm signup edit optional)","problem":"4.1b and 4.5 require `{{ .Token }}` in the Confirm signup template too (invited newcomers without an account receive it), and 4.5 has to send the founder back into Supabase to fix it.","fix":"Make both template edits required in 2.4's Auth step and in the helper prompt's checklist so 4.5 only verifies."},
{"step":"3.10","severity":"major","category":"migration-numbering","quote":"if `9998_*` exists use `9997_*`, and so on","problem":"Decision 21 reserves 9998 (5.6a), 9997 (6.1a) and 9996 (6.3a case B); a 3.10 round following this text takes a reserved number and collides with a later step's file.","fix":"'Any 3.10 migration takes the next free number from 9995 downward (check the HANDOFF ledger and `supabase/migrations/`); never 9998/9997/9996.' Also reuse `scripts/web/smoke-hosted.sh <URL>` (2.7) to verify the deployed fix."},
{"step":"3.5b","severity":"minor","category":"migration-numbering","quote":"9998 is reserved for join codes and 9997 for surveys — ask me if the ledger does not say which number is free","problem":"9996 is reserved too (6.3a case B); decision 21 fixer wording is 'next free number below the reserved block'.","fix":"'the next free number from 9995 downward (9998/9997/9996 are reserved)'."},
{"step":"3.5b","severity":"minor","category":"stale-fact","quote":"`notifications` select policy = any `announcements.*` holder ONLY (DESIGN §6; 1.4a) — a leader holding `bulletins.upload` without an `announcements.*` permission reads zero outbox rows","problem":"Decision 21 widens `ca_notifications_select` to `bulletins.upload` holders for `kind = 'bulletin'`; the Push column's visibility rule (`canSeePush`) is built on the old policy.","fix":"Show the Push column to `bulletins.upload` holders too and drop the 'reads zero outbox rows' reasoning (once 1.4a carries the widened policy)."},
{"step":"3.6b","severity":"minor","category":"stale-fact","quote":"`srv_process_account_deletions()` deletes the auth users of pending requests and stamps `processed_at`","problem":"Decision 15 / 1.3b: it processes only rows with `processed_at is null and scheduled_for <= now()` (30-day grace). The numbering note also omits 9996.","fix":"Add 'whose `scheduled_for` has passed' and the 9996 reservation."},
{"step":"5.6a","severity":"minor","category":"migration-numbering","quote":"If `9998` is already taken … name this file with the next free number (`9997_join_codes.sql`), tell me in your final message, and record in HANDOFF that G14's `9997_surveys_head_of_house.sql` must shift down one.","problem":"Decision 21 reserves 9998 for this step and tells the fixer to remove the shift contingency.","fix":"Delete the sentence; keep 'the file is `9998_join_codes.sql` (reserved by decision 21); do not edit 9999'."},
{"step":"1.4a","severity":"minor","category":"migration-numbering","quote":"bucket rows are upserted, so a later `9998_*` paste can raise it to 2 GiB on Pro","problem":"9998 is reserved for join codes; decision 21 names the worked example `9995_storage_limit.sql`.","fix":"Write `9995_storage_limit.sql` here and in the MIGRATIONS.md rule ('every new table created by a later migration…')."},
{"step":"1.6b","severity":"minor","category":"migration-numbering","quote":"so an additive `9998_*` migration never breaks deployed clients","problem":"Generic 'later migration' is spelled with the reserved 9998 (twice).","fix":"Write 'a later `999x_*` migration'."},
{"step":"1.6b","severity":"minor","category":"handoff-fact","quote":"decision 7 — 30-day trial (`trial_ends_at` defaulted by the UI)","problem":"1.2 gives the column `default now() + interval '30 days'` (recorded as a deviation); 2.2a relies on it.","fix":"Write 'defaulted by the DB (1.2), overridable in `/admin`'."},
{"step":"1.7a","severity":"minor","category":"migration-numbering","quote":"for a `9998_*` follow-up","problem":"Reserved number used as the generic 'later migration'.","fix":"'for a later `999x_*` migration (next free below 9996)'."},
{"step":"2.2b","severity":"minor","category":"migration-numbering","quote":"record it in HANDOFF \"Open questions\" for a `9998_*` fix","problem":"Reserved number used generically (twice in 2.2b).","fix":"'for a later migration (next free number below 9996)'."},
{"step":"2.3","severity":"minor","category":"migration-numbering","quote":"a `9998_*` migration is a later, founder-approved step","problem":"Reserved number used generically.","fix":"'a later `999x_*` migration (next free number below 9996)'."},
{"step":"2.4","severity":"minor","category":"migration-numbering","quote":"every later change is a new `9998_*` file","problem":"Checklist and helper prompt say every later change is `9998_*`; 9998 is `9998_join_codes.sql` (5.6a). The founder would expect that file name for any fix.","fix":"'every later change is a new descending file: 9998 join codes (5.6a), 9997 surveys (6.1a), 9996 account archival (6.3a case B), anything else from 9995 downward'."},
{"step":"2.6","severity":"minor","category":"migration-numbering","quote":"a real schema fix is a new `9998_*` re-runnable file and a founder paste","problem":"Reserved number used generically (twice). Also decision 24: 2.6 reuses 2.7's hosted smoke script but never mentions it.","fix":"'a new `999x_*` file (next free below 9996)'. Add to Inputs/Build: 'run `scripts/web/smoke-hosted.sh https://<VERCEL_URL>` (2.7) first and paste its FAIL lines into the triage log'."},
{"step":"3.3b","severity":"minor","category":"migration-numbering","quote":"raised by a later `9998_*` paste on Pro","problem":"The Pro-limit raise is `9995_storage_limit.sql` (decision 21); 9998 is join codes (twice in 3.3b).","fix":"Write `9995_storage_limit.sql`."},
{"step":"3.7b","severity":"minor","category":"migration-numbering","quote":"for a `9998_*` migration and mark the step as `test.fixme`","problem":"Reserved number used generically.","fix":"'for a later migration (next free number below 9996)'."},
{"step":"4.6","severity":"minor","category":"migration-numbering","quote":"file it as `later` with the proposed `9998_*` file name and stop","problem":"Reserved number used generically.","fix":"'with the next free `999x_*` name below 9996'."},
{"step":"3.7a","severity":"major","category":"unused-dependency","quote":"MISSING","problem":"3.7a lists `2.7` in depends_on (decision 24: '3.7a depends on it') but its Inputs and Build never mention 2.7's `scripts/web/smoke-hosted.sh` or the `workflow_dispatch`/`deployment_status` GitHub Actions job; the Playwright smoke suite re-implements the same header/redirect/404 checks without reference.","fix":"Add 2.7's outputs to Inputs and state the split: the shell script is the anonymous hosted check (reused by 2.6/3.9/3.10), Playwright covers rendering/validation with the mock env; `BASE_URL` runs of the smoke suite must not duplicate the script's assertions and the CI job from 2.7 stays. Alternatively drop `2.7` from depends_on — but decision 24 prefers the reuse."},
{"step":"3.9","severity":"minor","category":"handoff-reuse","quote":"Part C operations checks and, if you have Playwright locally, `BASE_URL=https://<VERCEL_URL> pnpm --filter web test:e2e:smoke`","problem":"Decision 24: 3.9 reuses 2.7's script, which needs only bash/curl.","fix":"Add 'run `scripts/web/smoke-hosted.sh https://<VERCEL_URL> --checkpoint B` → all PASS' before the optional Playwright run."},
{"step":"6.3b","severity":"major","category":"document-order","quote":"depends_on: ['6.3a', '6.3c']","problem":"The assembler sorts by id, so 6.3b (monitoring) appears before 6.3c (mobile account screens) although it depends on it; the 'How to use' rule is 'Work top to bottom … do not start a step before its dependencies are ticked'.","fix":"Swap the ids: mobile account screens become 6.3b, monitoring 6.3c; update depends_on in 6.3b/6.3c/6.4/6.5/6.6 and the text cross-references — or make `assemble.py` order each milestone topologically (stable by id)."},
{"step":"6.4","severity":"major","category":"document-order","quote":"depends_on: ['4.5', '6.3b', '6.6', '6.7']","problem":"6.4 and 6.5 depend on 6.6 (privacy policy live) and 6.7 (Phase 3 paste), which sort after them in docs/TODO.md; a top-to-bottom reader reaches the store steps before their prerequisites.","fix":"Renumber so prerequisites sort first (6.4 privacy policy, 6.5 Phase 3 paste, 6.6 iOS, 6.7 Play Console, 6.8 unchanged) and update every cross-reference and `depends_on` — or make `assemble.py` print steps in topological order within a milestone and say so in 'How to use'."},
{"step":"6.4","severity":"minor","category":"handoff-attribution","quote":"6.3a: `app/account/delete.tsx`, Deactivate/Delete rows on More (decision 15) … pasted (9999, and 9998/9997 if they exist","problem":"The mobile delete screen and More rows are 6.3c's (6.3a is web); the paste list omits 9996 (6.3a case B).","fix":"Attribute to 6.3c and add 9996 to the checklist's paste sentence."},
{"step":"6.2a","severity":"minor","category":"missing-dependency","quote":"6.1b shipped a CSV export for survey results — find its writer with `git grep -n \"text/csv\" apps/web` and reuse it; if 6.1b inlined the CSV code, extract it to `src/lib/csv.ts` … and re-point 6.1b's route at it","problem":"depends_on is `['3.6b']` only, yet the Build assumes 6.1b exists and edits its route.","fix":"Add `6.1b` to depends_on (it sorts earlier anyway) or make the sentence conditional ('if 6.1b has run')."},
{"step":"6.6","severity":"minor","category":"dependency-mismatch","quote":"Step 6.3b (if it ran) changed the privacy page's \"Service providers\" paragraph","problem":"6.6 depends on 6.3b, so it has run; the conditional contradicts depends_on.","fix":"Write 'Step 6.3b changed …' and drop '(if it ran)'."},
{"step":"6.8","severity":"minor","category":"handoff-name","quote":"the constants file `apps/web/src/lib/mobile-app.ts` (6.4/5.7: `ANDROID_PACKAGE`, `IOS_BUNDLE_ID`)","problem":"5.7a exports `ANDROID_PACKAGE_ID` from `@church/domain` `packages/domain/src/urls.ts`; no web `mobile-app.ts` exists in any prompt.","fix":"Reference `ANDROID_PACKAGE_ID` in `@church/domain` `urls.ts` (5.7a) and have 6.4 add `IOS_BUNDLE_ID` beside it."},
{"step":"5.9","severity":"minor","category":"missing-dependency","quote":"Checkpoint C (4.5) is done: the 4.5 preview APK signed in on your phone","problem":"5.9 requires 4.5 but depends_on lists only 5.7c and 5.8; 4.5 is not reachable transitively (5.7a depends on 4.4, not 4.5).","fix":"Add `4.5` to 5.9's depends_on."},
{"step":"4.1a","severity":"major","category":"stale-decision","quote":"the package id in `app.config.ts` is `com.anacast.app` with a comment that the founder gave `com.Anacast.app` … Mirror that here with `src/theme/tokens.ts` … (orchestrator decision 5: brand colours are still open","problem":"Decision 18 confirmed `com.anacast.app` (no case caveat anywhere) and decision 17/32 fixed the five-blue palette with token roles; 4.1a would write a stale comment and neutral tokens that 6.5's store assets and the splash are supposed to derive from.","fix":"Drop the `com.Anacast.app` comment/HANDOFF-lookup paragraph; `src/theme/tokens.ts` holds `ink #00072d`, `primaryDark #001c55`, `primary #0a2472`, `accent #0e6ba8`, `tint #a6e1fa` with the decision-17 usage rules (never Icy as text on white), used for splash `backgroundColor` and adaptive-icon background."},
{"step":"2.1a","severity":"major","category":"stale-decision","quote":"ONE `@theme` block holding the neutral colour tokens (orchestrator decision 5: brand colours are open — a single token file, neutral default","problem":"Decision 17 (confirmed by 32) replaces the neutral default with the brand palette; every later web step (3.7c contrast audit, 5.8 landing page, 6.5 assets) assumes the tokens are the brand values.","fix":"The `@theme` block defines `--color-primary #0a2472`, `--color-primary-dark #001c55`, `--color-ink #00072d`, `--color-accent #0e6ba8`, `--color-tint #a6e1fa` with the contrast notes from decision 17; error/success/warning stay framework defaults (documented in the file)."},
{"step":"3.7c","severity":"minor","category":"stale-decision","quote":"Theme: 2.1a keeps the neutral colour tokens in ONE `@theme` block … (orchestrator decision 5: brand colours are open","problem":"Stale after decision 17 (also 'neutral theme' in 3.10, 4.6, 5.1a).","fix":"Say 'the brand token block (decision 17)'; measure contrast against those values (Marine needs large text; Icy never as text on white)."},
{"step":"1.6a","severity":"minor","category":"stale-conditional","quote":"If 1.4 also created `packages/domain/fixtures/persons.json`, keep its shape … `docs/RLS.md` (step 1.5) has a \"Permission mapping\" section when 1.5 has already run","problem":"1.4c creates the fixture (its Build 1) and 1.5 sorts before 1.6a, so both conditionals are dead; 1.6a's depends_on omits 1.5 although it copies `docs/RLS.md` wording.","fix":"State both as facts (fixture exists; copy the mapping text from `docs/RLS.md`) and add `1.5` to depends_on."},
{"step":"0.1","severity":"minor","category":"finish-block","quote":"MISSING","problem":"The founder checklist ends with 'Record in HANDOFF.md' but never tells the founder to tick 0.1 in `docs/TODO.md` (every other founder step does).","fix":"Add a final item: 'Tick step 0.1 in `docs/TODO.md` (or let the helper prompt do it) and push.'"},
{"step":"NEW","severity":"minor","category":"assembler-header","quote":"brand colours and production domain deferred (neutral theme, `NEXT_PUBLIC_APP_URL`) … confirm in step 0.1 … not explicitly confirmed by the founder … after that every change is a new `9998_*` file.","problem":"`assemble.py`'s 'Decisions these prompts assume' list predates decisions 17/18/21/33: palette and lowercase id are confirmed, the permission mapping is accepted, and later migrations are 9998 join codes / 9997 surveys / 9996 archival / 9995 downward.","fix":"Update the four bullet strings in `assemble.py` and regenerate docs/TODO.md."}
]}
