{
  "group": "G2",
  "applied": 41,
  "rejected": [
    {
      "finding": "completeness / 1.3a major 'founder-decision-missing': relax `guard_persons_columns` so a household adult may set any `household_role` (incl. `head`/`spouse`) on persons of their own household directly, with a matching smoke test and done_when clause.",
      "reason": "Conflicts with orchestrator decision 21, which makes G12's rejected step 5.8 the specification for the head-of-house fix: that spec says `save_person` must NOT be relaxed for `head`/`spouse` on others and that the only way to move the head is the atomic `set_head_of_house` RPC (option (i): a session flag set by the RPC). Decision 6 / founder answer 8 ('editable by directory admins and by the household') is satisfied instead by 1.3b's new `set_head_of_house(hh, new_head, previous_head_role)` — admitted through `app.can_edit_household`, so household adults can use it — running under the new `app.trusted_rpc` flag. The guard stays strict; the smoke test asserts `household_role_not_allowed` for the direct update and the successful swap through the RPC. Recorded in the 1.3a notes and in open_issues."
    }
  ],
  "validator_errors": 0,
  "open_issues": [
    "File revised in place (not committed): /home/user/ChurchAppCLD/Session log/003_2026-09-04/todo-drafts/groups/G2.json — validator prints 0 errors, 3 warnings (prompt length: 1.2 = 5415 words, 1.3a = 4564, 1.3b = 4708). Decision 26 accepted 1.2 at 4040; the orchestrator should extend it to 1.3a/1.3b or let the global fixers remove repetition without cutting content.",
    "G3 propagation (1.4a/1.4b/1.4c/1.5): the shim tweaks 1.4a Build 6 planned (`service_role` BYPASSRLS, RLS on `storage.objects`, storage table grants, re-apply shim every run) are now done in 1.2 — reduce 1.4a to 'verify'; replace `psql \"$(cat .local-pg/PG_SUPER_URL)\"` with the new `super_psql` helper from `scripts/db/_env.sh` (Path A has no superuser URL); grant the changed/new signatures `srv_claim_notifications(uuid, int)`, `set_head_of_house(uuid, uuid, household_role)`, `srv_load_unlocked_share_link(uuid)`, `deactivate_account()`, `restore_my_account()`, `srv_archive_account(uuid, text)`, `srv_restore_account(uuid)` (public count is 30, not 24); `ca_events_insert` must consult `calendars.members_can_submit`; tests should cover `app.trusted_rpc`/`app.cascade` bypasses, the check-first rate-limit semantics (10th wrong = wrong_password, 11th = rate_limited), the archival RPCs, `cancelled_at`, and the full trigger-name list.",
    "G4 propagation (1.6/1.7): `errors.ts` loses the six `share_link_*` keys and gains `system_group_immutable`, `calendar_submissions_closed`, `account_archived`, `account_archived_by_admin`, `deletion_pending`; share-link outcomes are a separate status block (`needs_password` now carries `link_id`, `sermon_title`, `org_name`); `rpc.ts` must use the by-name parameters listed in 1.3b/open_issues (e.g. `srv_audience_user_ids(group_id, org)`, `srv_claim_notifications(org_id, max_rows)`); `ACCOUNT_DELETION_GRACE_DAYS = 30` lives in `packages/domain/src/limits.ts`.",
    "G8 (3.4b), G9 (3.5b/3.6a), G12 (5.1a/5.2b/5.3a/5.3b), G13 (5.5), G14 (6.3a) now have their decision-21/15 database pieces in 9999: 3.4b should call `srv_load_unlocked_share_link(link_id)` and show 'sharing disabled' for suspended orgs; 3.6a passes `{ org_id, max_rows }` and 3.5b's 'publish_at edit does not move scheduled_for' notice is obsolete; 5.1a/5.2b call `set_head_of_house` (drop the `TODO(5.8)` comments) and 5.3 surfaces `calendar_submissions_closed`; 5.5 needs no policy trick for system groups; 6.3a runs in case A with exactly the names it already uses (incl. `restore_my_account()`, which I added beyond the fidelity critic's list to match G14).",
    "Name flagged per decision 21: `srv_load_unlocked_share_link(link_id uuid)` (G8's stub called it `srv_load_share_link(link_id, token_hash)`); also the internal helpers `app.archive_account(user_id, reason, actor)` and `app.share_link_payload(link_id)` are new names chosen in 1.3b.",
    "Deviation from the fidelity critic's fix text: `set_head_of_house` uses the G12 5.8 signature `(hh uuid, new_head uuid, previous_head_role household_role default 'spouse')` rather than the critic's `(person_id uuid)`, and the function counts are 20 client + 10 srv = 30 (the critic wrote 21 + 10 = 31).",
    "Recorded deviations from DESIGN the agents must write into HANDOFF: `trial_ends_at` column default (+30 days), `organizations.cancelled_at`, raise convention (message = key), `pg_trigger_depth() > 1` instead of `> 0`, archived users as non-members, `sharing_disabled` when the org is not writable, `srv_audience_user_ids(group_id, …)`, storage helpers reading the directory views, section (5c) inside section (5)."
  ]
}