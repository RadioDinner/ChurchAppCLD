{"findings": [
{"step":"1.4a","severity":"blocker","category":"rpc-names","quote":"section (9) revokes PUBLIC execute and grants exactly the 17 client RPCs and the policy/view helpers to `authenticated` and the 7 `srv_*` RPCs to `service_role`","problem":"G2 1.3b (revised for decisions 15/21) now defines 20 client RPCs (adds `deactivate_account`, `restore_my_account`, `set_head_of_house`) and 10 `srv_*` RPCs (adds `srv_load_unlocked_share_link`, `srv_archive_account`, `srv_restore_account`). 1.4a still lists 17 + 7 by name, so after its `revoke execute on all functions in schema public from public, anon, authenticated` the three new client RPCs are unexecutable by `authenticated`, the three new service RPCs unexecutable by `service_role`, and `09_grants.sql` never asserts them. 6.3a, 5.1a and 5.2b then fail at runtime.","fix":"In 1.4a Build (section 9) and in the `09_grants.sql` description replace '17 client RPCs' with the 20 names exactly as 1.3b lists them (append `deactivate_account()`, `restore_my_account()`, `set_head_of_house(uuid, uuid, public.household_role)`) and '7 `srv_*` RPCs' with the 10 names (append `srv_load_unlocked_share_link(uuid)`, `srv_archive_account(uuid, text)`, `srv_restore_account(uuid)`); update the Done-when counts and the `has_function_privilege` assertions accordingly."},
{"step":"1.4a","severity":"major","category":"policy-names","quote":"MISSING","problem":"Decision 21 puts the `ca_notifications_select` widening (also visible to `bulletins.upload` holders where `kind = 'bulletin'`) into 9999 via the G2/G3 fixers. Policies are section (8), owned by 1.4a, but neither 1.4a nor 1.4b/1.4c names `ca_notifications_select` or the bulletin branch, while 3.5b/3.6a already show the bulletin outbox row to bulletin uploaders.","fix":"Add to 1.4a's policy catalogue: `ca_notifications_select` for `authenticated` = `org_id = any(app.perm_org_ids('announcements.create')) or (kind = 'bulletin' and org_id = any(app.perm_org_ids('bulletins.upload')))` (use the exact helper names from 1.3a), and add one assertion to 1.4c's `06_events_surveys.sql` notification block: a `bulletins.upload`-only user sees the bulletin outbox row and not an announcement row."},
{"step":"1.4c","severity":"blocker","category":"rpc-signature","quote":"`srv_claim_notifications(int)` … `srv_claim_notifications(10)` → returns the queued rows","problem":"1.3b defines `srv_claim_notifications(org_id uuid, max_rows int)` (decision 21). 1.4c's grant list and behavioural test call the one-argument form, which will not exist, so `09_grants` and `06_events_surveys` fail against the schema 1.3b produces.","fix":"Change every mention in 1.4c to `srv_claim_notifications(uuid, int)` in the grant/signature list and `srv_claim_notifications(null, 10)` / `srv_claim_notifications(<org A id>, 10)` in the behavioural test; add one assertion that the org-scoped call claims only that org's rows."},
{"step":"1.7a","severity":"major","category":"rpc-names","quote":"`rpc.ts` with a typed wrapper for each of the 17 client RPCs and 7 `srv_*` RPCs … `grep -c \"^export async function srv\" packages/db/src/rpc.ts` → `7`","problem":"1.3b defines 20 client and 10 `srv_*` RPCs. 1.7a's counts and grep checks contradict the generated `database.types.ts`, and 5.1a/5.2b/6.3a/3.4b expect wrappers `setHeadOfHouse`, `deactivateAccount`, `restoreMyAccount`, `srvLoadUnlockedShareLink`, `srvArchiveAccount`, `srvRestoreAccount` that 1.7a never asks for; `srvClaimNotifications` must take `{ org_id, max_rows }`.","fix":"Change to '20 client RPCs and 10 `srv_*` RPCs', list the six additional wrapper names, make the verify greps `→ 10` (srv) and the client count `→ 20`, and state that `srvClaimNotifications(client, { org_id, max_rows })` mirrors 1.3b's two-argument signature."},
{"step":"3.6a","severity":"blocker","category":"rpc-signature","quote":"There is no org option: `srv_claim_notifications` takes only `max_rows`, so a synchronous run after publish may also deliver other churches' due rows — harmless and documented","problem":"Decision 21 folded G9's proposal into 9999: 1.3b defines `srv_claim_notifications(org_id uuid, max_rows int)` with per-org claiming. 3.6a builds `dispatchQueued` against a one-argument RPC that does not exist and documents a cross-tenant side effect the schema already prevents; it also still points at the rejected proposal 3.12.","fix":"Rewrite the dispatcher section: `dispatchQueued({ orgId, maxRows })` calls `srvClaimNotifications(svc, { org_id: orgId ?? null, max_rows })`; the synchronous after-publish path passes the publishing org's id, the cron passes `null`; delete the 'no org option' sentences, the 'other churches' rows' caveat and the reference to proposed 3.12; add a Vitest case that after-publish dispatch passes `org_id`."},
{"step":"6.2b","severity":"minor","category":"rpc-signature","quote":"`srv_claim_notifications(max_rows)` claims `queued` rows with `scheduled_for <= now()` and `attempts < 5`","problem":"Same RPC, written with the pre-decision-21 one-argument signature; 1.3b/3.6a (after fix) use `(org_id, max_rows)`.","fix":"Write `srv_claim_notifications(org_id, max_rows)`."},
{"step":"3.4b","severity":"major","category":"rpc-names","quote":"cookie-verified visits are served through an additive `getShareLinkForCookieVisit` query re-validated in TypeScript … (candidate `srv_load_share_link` for a later migration — proposed step 3.11)","problem":"Decision 21 rejected proposal 3.11 and put `srv_load_unlocked_share_link(link_id uuid) returns jsonb` (service role) into 9999 via 1.3b, 'so 3.4b stops re-validating in TypeScript'. 3.4b still builds the TS re-validation path, names the RPC `srv_load_share_link`, adds an unneeded `@church/db` query and tells the agent to record a proposal that no longer exists.","fix":"Replace the cookie-visit design with: `loadUnlockedShare(linkId)` calls `srvLoadUnlockedShareLink(svc, { link_id })` (wrapper from 1.7a) and maps its status strings (`ok`, `invalid`, `revoked`, `expired`, `max_views`, `sharing_disabled`, `unpublished` — copy 1.3b's list) to the page states; drop `getShareLinkForCookieVisit`, its packages/db test, the TS re-checks, the `srv_load_share_link` name and every 'proposed step 3.11' sentence (Build, Constraints, Verify, Finish/HANDOFF)."},
{"step":"5.1a","severity":"blocker","category":"head-of-house","quote":"head-of-house is two `save_person` calls pending the atomic `set_head_of_house` RPC (proposed step 5.8)","problem":"Decision 30 settles the rule: the column guard stays strict and 'the only way to move the head is the atomic `set_head_of_house(hh, new_head, previous_head_role)` RPC' defined in 1.3b (decision 21 folded proposal 5.8 into 9999 and rejected it). Two `save_person` calls are refused by `guard_persons_columns` / the partial unique index, so the web head-of-house control cannot work as written.","fix":"Make the head-of-house control call `setHeadOfHouse(db, { hh, new_head, previous_head_role })` (1.7a wrapper of 1.3b's `set_head_of_house(hh uuid, new_head uuid, previous_head_role public.household_role default 'spouse')`), with a role picker for the demoted head (`spouse` default, `other`); remove the 'two save_person calls', the 'pending proposed 5.8' wording and the unique-violation fallback message; keep 5.1b's 'Use Set head of house on the household page' redirect."},
{"step":"5.2b","severity":"blocker","category":"head-of-house","quote":"designate the head of house (two-call sequence, gracefully handling the 1.3a `household_role_not_allowed` refusal) … record in HANDOFF \"Deviations from plan\" that 1.3a's guard blocks household adults","problem":"Same as 5.1a: decision 30 makes `set_head_of_house` (admitted through `app.can_edit_household`, so household adults may call it) the only path; the two-call `save_person` sequence and the 'ask your church' fallback describe a conflict that 1.3b already resolved.","fix":"Call `setHeadOfHouse` from the mobile household screen with the same arguments as 5.1a; delete the two-call sequence, the `household_role_not_allowed` fallback copy, the 'record which happened in HANDOFF' instruction and the notes paragraph about the 1.3a conflict."},
{"step":"4.1a","severity":"major","category":"package-id","quote":"`android.package: 'com.anacast.app'` preceded by a comment block: \"Founder gave `com.Anacast.app` (HANDOFF …) …\" unless HANDOFF records a different confirmed value","problem":"Decision 18 (2026-09-07) confirms `com.anacast.app` and says 'prompts use the lowercase id everywhere … with no case caveat'; 0.1 already records it as confirmed. 4.1a still carries the `com.Anacast.app` comment, the HANDOFF answer-9 quotation and the 'use a different confirmed value' branch.","fix":"Set `android.package: 'com.anacast.app'` with a one-line comment 'confirmed by the founder 2026-09-07; immutable after the first Play upload'; delete every `com.Anacast.app` mention, the HANDOFF answer-9 quotation and the conditional branch in Build, Constraints, Verify and notes."},
{"step":"4.5","severity":"minor","category":"package-id","quote":"`android.package` exactly — if the founder confirmed `com.Anacast.app` in 0.1 …","problem":"Stale case caveat (decision 18); the value is fixed.","fix":"Replace with 'the package name is `com.anacast.app` exactly (confirmed 2026-09-07)'."},
{"step":"3.10","severity":"major","category":"migration-numbering","quote":"if `9998_*` exists use `9997_*`, and so on","problem":"Decision 21 reserves 9998 (join codes, 5.6a), 9997 (surveys, 6.1a) and 9996 (account archival, 6.3a case B); a 3.10 round that needs a migration must take the next free number from 9995 downward. As written, a 3.10 agent takes 9998 or 9997 and collides with the reserved files. Also contradicts 1.5's docs/MIGRATIONS.md reservation table.","fix":"Replace with: 'name it with the next free number below the reserved block — 9998/9997/9996 are reserved for `9998_join_codes.sql`, `9997_surveys_head_of_house.sql` and `9996_account_archival.sql`; start at 9995 and count down, checking the HANDOFF ledger and docs/MIGRATIONS.md reservation table (the worked example `9995_storage_limit.sql` may already exist)'."},
{"step":"3.5b","severity":"major","category":"migration-numbering","quote":"as the next free descending number recorded in the HANDOFF ledger (9998 is reserved for join codes and 9997 for surveys — ask me if the ledger does not say which number is free)","problem":"Omits the 9996 reservation and the 'from 9995 downward' rule of decision 21, so the fallback migration could take 9996 (reserved for 6.3a case B).","fix":"Write: 'as the next free number below the reserved block (9998 join codes, 9997 surveys, 9996 account archival are reserved) — start at 9995 and count down, consulting the docs/MIGRATIONS.md reservation table and the HANDOFF ledger'."},
{"step":"5.6a","severity":"major","category":"migration-numbering","quote":"If `9998` is already taken … name this file with the next free number (`9997_join_codes.sql` …) … tells the founder that G14's 9997 shifts","problem":"Decision 21 reserves 9998 for this step and says 5.6a/6.1a 'lose the if-taken contingencies'. The contingency would let an agent rename the file and shift 6.1a's reserved 9997, contradicting 1.5's reservation table, 5.9's checklist ('paste `9998_join_codes.sql`') and 6.7.","fix":"Delete the contingency sentences in Build, Done-when and notes; state once that the file is `supabase/migrations/9998_join_codes.sql` (reserved by orchestrator decision 21) and that any other pending migration takes 9995 downward."},
{"step":"6.1a","severity":"major","category":"migration-numbering","quote":"if the ledger shows 9997 taken, use the next free lower number and record it","problem":"Same contingency decision 21 removes; a shift would break 6.7's checklist (`9997_surveys_head_of_house.sql`) and 1.5's reservation table.","fix":"Delete the contingency; the file is `9997_surveys_head_of_house.sql`, reserved by decision 21."},
{"step":"6.4","severity":"major","category":"file-paths","quote":"the token file `apps/mobile/src/theme.ts` is the single source","problem":"4.1a splits the mobile theme into `src/theme/tokens.ts` (plain constants, no imports — the file `app.config.ts` and Node scripts can load) and `src/theme.ts` (re-export plus the react-native-paper theme). A `scripts/assets/` Node script generating store assets from `src/theme.ts` pulls react-native-paper and fails; the colour VALUES live in `tokens.ts`.","fix":"Name `apps/mobile/src/theme/tokens.ts` as the single source of the hex values (and `src/theme.ts` only as the Paper theme that re-exports it) in Build and the founder checklist."},
{"step":"6.5","severity":"major","category":"file-paths","quote":"the token file `apps/mobile/src/theme.ts` is the single source","problem":"Same divergence as 6.4 from 4.1a's `src/theme/tokens.ts` / `src/theme.ts` split.","fix":"Point the asset generator at `apps/mobile/src/theme/tokens.ts`."},
{"step":"6.8","severity":"major","category":"store-listing","quote":"adds store badges to the invite page, landing page and invitation email behind `NEXT_PUBLIC_PLAY_STORE_URL` / `NEXT_PUBLIC_APP_STORE_URL`","problem":"5.8 already gives the `/join/[code]` landing page a domain constant `PLAY_LISTING = { live: false as boolean, url: playStoreUrl() }` (comment 'step 6.8 flips this') that decides between the Play button and the 'not yet listed' sentence. 6.8 never mentions `PLAY_LISTING` or `/join/[code]`, so after release the join landing page keeps saying the app is not listed while the invite page shows a badge — two mechanisms for one fact.","fix":"Pick one mechanism and use it in both steps: either 6.8's Build adds 'set `PLAY_LISTING.live = true` in `@church/domain` (5.8) and add the join landing page to the badge smoke test', or 5.8 reads `NEXT_PUBLIC_PLAY_STORE_URL` through `publicEnv()` instead of a constant and 6.8's env var doubles as the switch. Update the other step's notes to match."},
{"step":"5.7c","severity":"minor","category":"playwright-layout","quote":"`apps/web/e2e/phase2.spec.ts` sweeps all Phase 2 routes","problem":"Decision 23 fixes feature smoke specs as `apps/web/e2e/<feature>.smoke.spec.ts` (collected by 3.7a's `testMatch: /smoke\\.spec\\.ts/`); `phase2.spec.ts` only runs because 5.7c widens `testMatch`, which decision 23 keeps merely as a safety net.","fix":"Rename to `apps/web/e2e/phase2.smoke.spec.ts` in Build, Verify, Done-when and notes."},
{"step":"2.7","severity":"minor","category":"package-names","quote":"`pnpm --filter ./apps/web build` … `pnpm --filter ./apps/web exec next start -p 3100 &`","problem":"Decision 2 fixes the filter form as `pnpm --filter web`; every other step uses it. The path form works but is the only divergent spelling.","fix":"Use `pnpm --filter web build` and `pnpm --filter web exec next start -p 3100 &`."},
{"step":"6.3b","severity":"minor","category":"commit-prefix","quote":"feat: error monitoring behind env (web + mobile), monitoring runbook, security audit script and checklist run-through","problem":"Only commit message without a scope; the skeleton's convention is `<prefix>(<scope>)`, and 5.7a uses `feat(web,mobile):` for a web+mobile step.","fix":"`feat(web,mobile): error monitoring behind env, monitoring runbook, security audit script and checklist run-through`."},
{"step":"1.1","severity":"minor","category":"finish-boilerplate","quote":"push `main`","problem":"Decision 12 fixes the Finish wording as 'push (`git push origin main` unless another branch was named)'. Steps 1.1, 1.3a, 1.3b, 1.4b, 1.4c, 1.5, 2.3, 2.6, 3.5a, 3.5b, 3.6a, 3.6b, 5.1a, 5.1b, 5.2a, 5.2b, 5.3a, 5.3b, 6.1a, 6.1b, 6.1c, 6.2a, 6.2b, 6.3a, 6.3b, 6.3c, 6.4, 6.5 and 6.6 write 'push `main`' or 'push `main` (or the branch I named)' instead; the other 40 agent prompts use the decided form.","fix":"In each listed step's Finish block write 'push (`git push origin main` unless another branch was named in this session)'."}
]}
