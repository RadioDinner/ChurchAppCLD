{"findings": [
{"step":"3.9","severity":"blocker","category":"founder-blocked","quote":"Optional: Resend for invitation emails → `RESEND_API_KEY`, `RESEND_FROM`. Only if you own a domain (step 0.1).","problem":"Supabase's built-in email sender delivers only to addresses that are members of the founder's Supabase organization (2.4 item 12 says so and defers custom SMTP to 3.9), but 3.9 never configures Supabase Auth SMTP — its Resend item only sets the app's invitation-mail key. Every later flow that sends a Supabase OTP/confirmation mail to someone else therefore silently fails: 3.9 Part B (`<FOUNDER_EMAIL>+admin` alias doing an Email-code sign-in), 4.5 item 23 (second family member), 5.9 item 19 (join flow with a fresh address), 6.5/6.8 reviewers and closed testers. The founder will read 'no email' as an app bug.","fix":"Add a mandatory 3.9 item before Part B: 'Supabase custom SMTP (required before anyone but you signs in by email): Supabase → Authentication → Emails → SMTP Settings → Enable Custom SMTP → host `smtp.resend.com`, port 465, user `resend`, password = a Resend API key, sender `Anacast <no-reply@<DOMAIN>>` (Resend needs a verified domain — if you have none yet, register one now; it is a prerequisite for Checkpoint B Part B, Checkpoint D and the store testers). Then Authentication → Rate Limits → raise emails per hour. Fallback without a domain: Supabase → Organization → Team → invite the test addresses as organization members so the built-in sender may deliver to them.' Update the goal/done_when ('custom SMTP configured or the team-member fallback recorded'), 2.4 item 12 ('pending: 3.9 item N'), 0.1 item 8.1 (domain 'optional' → 'needed before 3.9 Part B'), and add 'custom SMTP is on (3.9)' to the preconditions of 4.5 and 5.9."},
{"step":"2.1a","severity":"blocker","category":"decision-not-applied","quote":"404s non-active members AND super admins without a membership (code comment references step 3.11)","problem":"Orchestrator decision 20 settled the pass-through rule (super admin with `membership_status = null` is admitted to `/o/<slug>/*` with a 'Viewing as super admin' strip) and told the G5 fixer to drop the `notFound()` rule and the 3.11 comment; the 2.1a goal, done_when and notes still build the 404. 3.1 then says it 'keeps step 2.1's rule … super admins pass', 3.7b flags the contradiction as unresolved, and 1.3b/2.1b/3.4b still point at a rejected step 3.11. The founder's Checkpoint B objective (trial the web app as super admin) depends on this.","fix":"Apply decision 20 in 2.1a (goal, Build, done_when, notes): `requireOrgMember(slug)` admits an active membership OR a super admin with `membershipStatus = null`; remove every '3.11' reference (also in 1.3b, 2.1b, 3.4b, 3.7b notes); 3.7b's 'unresolved super-admin rule' line becomes 'pass-through per decision 20'."},
{"step":"3.10","severity":"blocker","category":"migration-numbering","quote":"if `9998_*` exists use `9997_*`, and so on","problem":"Decision 21 reserves 9998 (join codes, 5.6a), 9997 (surveys, 6.1a) and 9996 (archival, 6.3a) and says anything else pasted after 9999 takes the next free number from 9995 downward; a 3.10 round that follows this sentence would consume a reserved number and collide with 5.6a/6.1a (whose 'shift if taken' contingencies are being removed).","fix":"Replace with: 'a NEW file `supabase/migrations/<n>_<topic>.sql` where `<n>` is the next free number below the reserved block (9998/9997/9996 are reserved for steps 5.6a/6.1a/6.3a — start at 9995 and count down; see the reservation table in docs/MIGRATIONS.md)'. Same wording in the notes."},
{"step":"2.4","severity":"major","category":"migration-numbering","quote":"From this row on `9999_init.sql` is frozen: every later change is a new `9998_*` file.","problem":"The checklist (item 8), the helper's Constraints ('any fix is a new `9998_*` file') and Build 3 ('later changes go to `9998_*`') tell the founder and the helper agent that the next migration is 9998 — but 9998 is reserved for 5.6a (decision 21). A hosted-paste fix after the freeze would take a reserved number.","fix":"In item 8, the helper Constraints and Build 3 write: 'every later change is a new re-runnable file numbered from 9995 downward (9998, 9997 and 9996 are reserved for steps 5.6a, 6.1a and 6.3a — docs/MIGRATIONS.md reservation table)'."},
{"step":"2.6","severity":"major","category":"migration-numbering","quote":"never edit `supabase/migrations/9999_init.sql` (frozen; a real schema fix is a new `9998_*` re-runnable file and a founder paste)","problem":"Same decision-21 collision as 2.4/3.10: Build 3 and the DEPLOY.md 'Supabase operations' section ('adding a migration = paste `9998_*`') hand the founder and later agents a reserved number.","fix":"Replace `9998_*` with 'the next free number below the reserved block (9995 downward; 9998/9997/9996 belong to 5.6a/6.1a/6.3a)' in Build 3, Build 5.4 and the notes; DEPLOY.md links docs/MIGRATIONS.md's reservation table."},
{"step":"3.3b","severity":"minor","category":"migration-numbering","quote":"how to raise them on Supabase Pro (Vercel env, a `9998_*` paste, the Storage settings page)","problem":"docs/ENV.md's 'Sermon uploads' section would tell the founder to raise the bucket limit with a `9998_*` paste; decision 21 and 1.5's worked example name that file `9995_storage_limit.sql`.","fix":"Write '`9995_storage_limit.sql` (docs/MIGRATIONS.md worked example)' instead of 'a `9998_*` paste' in done_when and the prompt."},
{"step":"2.4","severity":"major","category":"dangling-reference","quote":"If `docs/TODO.md` contains a step 1.8 (throwaway-project dry run of `9999_init.sql`), do it first","problem":"Step 1.8 was rejected by decision 24 and folded into 2.4 'as an optional dry-run item'; the founder is sent to look for a step that does not exist, and the notes still 'endorse' 1.8.","fix":"Replace the sentence with an optional item 0: 'Dry run (recommended, 15 min): create a throwaway Free project `anacast-dryrun` in the same organization (Free allows two active projects), paste `9999_init.sql` twice, run the item-6 queries, then Project Settings → General → Delete project. Any error goes to the helper prompt before you create the real project.' Update the notes accordingly."},
{"step":"2.4","severity":"major","category":"prerequisite-order","quote":"**Confirm signup**: keep `{{ .ConfirmationURL }}`; you may add `{{ .Token }}` too","problem":"4.5 ('required here … 2.4 made the second optional'), 5.6c, 5.9 item 19 and 4.1b all require `{{ .Token }}` in the Confirm signup template because invited or joining newcomers receive that template on their first OTP. Leaving it optional in 2.4 guarantees a second dashboard visit and a confusing 'link instead of code' failure at Checkpoint C/D.","fix":"Make item 10.2 mandatory: 'Confirm signup: the body MUST contain `{{ .Token }}` (newcomers who sign in with a code receive this template first — step 4.5/5.9 depend on it); suggested body: `<h2>Confirm your Anacast account</h2><p>Your code is <strong>{{ .Token }}</strong>…</p>`'. Add it to the Done-when line and to the helper's Auth inputs; 4.5 item 1 then only re-checks."},
{"step":"2.4","severity":"major","category":"free-plan-behaviour","quote":"If the dashboard later shows \"Project paused\", click **Restore** and wait ~2 minutes; Vercel keeps serving pages but every DB call fails until then.","problem":"Supabase's one-click Restore is only available for about 90 days after a pause; after that the founder must download a backup and restore it into a new project by hand. The build has multi-week gaps (Apple enrolment, Google's 14-day closed test, lawyer review of 6.6), so this limit will bite exactly when the founder is not looking. Neither 2.4, 2.6's symptom table nor DEPLOY.md mention it.","fix":"Extend item 14: 'Restore works for about 90 days after the pause; after that only a manual backup-and-restore into a new project is possible (new URL and keys). Rule: open the dashboard at least every two weeks while the project is idle, and always after a gap (before 3.9, 4.5, 5.9, 6.7, 6.8). Free also allows only two active projects.' Add the same to 2.6's 'Project paused' row and DEPLOY.md 'Supabase operations', and to HANDOFF Environment status item 1."},
{"step":"2.5","severity":"major","category":"decision-not-applied","quote":"invite himself as that church's first admin with all 12 permissions","problem":"Decision 20 told the G6 fixer that 2.5 stops inviting the founder into the first church (super admins reach `/o/<slug>/*` directly from 3.1 on); the goal, checklist item 11.4, done_when and notes ('Per G7's open issue, super admins are not org members … so the M3 org pages work for him') still do it and explain the obsolete reason. 2.2b's done_when likewise records an 'invite yourself' founder to-do for 2.5.","fix":"11.4: 'Admins tab → Invite a second address you control with all 12 permissions (Resend is not configured, so copy the link and accept it in a private window) — as super admin you will open every church directly once 3.1 ships; you only need to be a member yourself for the phone trial in 4.5.' Keep the 'Create account directly' exercise for a third address. Update goal/done_when/notes; delete the 'invite yourself' to-do from 2.2b's done_when/notes and the 'super-admins-are-not-members note' wording in 2.1b."},
{"step":"0.1","severity":"major","category":"stale-decision","quote":"Confirm or change the suggested token roles — `primary` Imperial Blue, `primaryDark` Deep Navy, `ink` Prussian Blue, `accent` Bright Marine, `tint` Icy Blue.","problem":"The token roles were confirmed on 2026-09-08 (decision 32b; HANDOFF's Brand palette table already says 'confirmed by the founder'), the production domain is settled as 'none yet — Vercel URL' (decision 31a), and HANDOFF's 'Still open for the founder' section now reads 'Nothing … Do not re-ask these'. Items 9.4, 9.5 and 11, the helper's Build 3–4 ('strike item 1 … item 5') and done_when items 6 re-ask them.","fix":"Items 9.4/9.5 become checks only ('already confirmed 2026-09-08 — verify HANDOFF → Brand palette third column' / 'domain deferred by decision: Vercel URL until features work — nothing to decide'); item 11 and the helper record only the account status lines; drop the 'Still open items 1 and 5' sentences from checklist, helper and done_when; update the goal and notes."},
{"step":"0.1","severity":"major","category":"founder-tooling","quote":"MISSING","problem":"Several founder checklists require the founder to run terminal commands on a computer with the repo: 2.5 (`openssl rand -base64 32`), 4.5 (`git pull`, `pnpm install`, `npx eas-cli login/init/build/credentials`), 5.9 (`eas credentials`, `adb shell pm get-app-links`, `curl`), 6.4/6.5/6.8 (`eas build`/`submit`). The skeleton only guarantees tooling on 'the founder's agents' machines'; nothing tells a non-developer what to install, where to run these, or that EAS/Apple logins are interactive (browser + 2FA) and cannot be done by an agent.","fix":"Add item 12 'Your computer': install Git, Node 22 (nvm) and pnpm 10, clone `RadioDinner/ChurchAppCLD`, install Android platform tools (`adb`) for 5.9, and note that `npx eas-cli …`, `adb` and `openssl` commands in later checklists run in a terminal at the repo root on this machine (an agent session on the same machine may run them too, but Expo/Apple logins need your browser and 2FA). Reference this item from 2.5 item 1, 4.5 item 4 and 5.9's intro."},
{"step":"6.5","severity":"major","category":"outdated-fact","quote":"a personal account created after November 2023 must run a closed test with 20 testers for 14 days","problem":"Google lowered the closed-test minimum from 20 to 12 opted-in testers on 11 December 2024 (Play Console Help 'App testing requirements for new personal developer accounts'); 0.1 says 12, 6.5 says 20 in checklist item 4, prompt Build 4, done_when and notes, and 6.8 says 'lowered to 12 in 2025'. The founder would recruit the wrong number and the three steps disagree.","fix":"Use '12 testers opted in for 14 consecutive days (20 before December 2024; the Console shows the current number)' in 6.5 checklist item 4, Build 4, done_when and notes."},
{"step":"6.8","severity":"minor","category":"outdated-fact","quote":"20 at the rule's launch, lowered to 12 in 2025","problem":"The reduction to 12 took effect on 11 December 2024, not 2025; goal, checklist item 4, Build 1(b) and notes repeat the wrong year.","fix":"Write 'lowered from 20 to 12 in December 2024' in all four places."},
{"step":"3.9","severity":"major","category":"missing-founder-action","quote":"Quick checks in a terminal: `curl -s -o /dev/null -w '%{http_code}\\n' -X POST https://<VERCEL_URL>/api/stripe/webhook` → `400`","problem":"Step 2.7 built `scripts/web/smoke-hosted.sh <URL> --checkpoint B` and a `deploy-smoke` GitHub workflow driven by the repository variable `SMOKE_CHECKPOINT` (A now, 'B after step 3.9'); 2.7's notes leave it to the fixer to make 3.9 flip the variable and use the script. 3.9 does neither, so after 3.9 every production deployment shows a red `deploy-smoke` check (webhook now answers 400, cron route exists) and the founder has no idea why.","fix":"Add to item 6: 'GitHub → `RadioDinner/ChurchAppCLD` → Settings → Secrets and variables → Actions → Variables → set `SMOKE_CHECKPOINT` = `B` (and create `PRODUCTION_URL` = `https://<VERCEL_URL>` if 2.7 did not get it)'; replace item 6.3's curl lines with `bash scripts/web/smoke-hosted.sh https://<VERCEL_URL> --checkpoint B` → exit 0; the helper's Build 1 runs the same script; add both to done_when."},
{"step":"2.7","severity":"minor","category":"missing-founder-action","quote":"under Environment status add the founder to-dos \"GitHub → Settings → Secrets and variables → Actions → Variables: create `PRODUCTION_URL` = `https://<VERCEL_URL>` and `SMOKE_CHECKPOINT` = `A`","problem":"The only founder action of an agent step is buried in a HANDOFF to-do and the agent's final message; the TODO step itself (goal/done_when) never tells the founder that two GitHub repository variables must exist for the `deploy-smoke` workflow to run on demand.","fix":"Add a one-line 'Founder action after this step' to the goal and a done_when item: 'the founder created the repository variables `PRODUCTION_URL` and `SMOKE_CHECKPOINT` (= `A`) under GitHub → Settings → Secrets and variables → Actions → Variables, and a manual `deploy-smoke` run is green'; 3.9 flips the checkpoint to `B`."},
{"step":"2.5","severity":"minor","category":"free-plan-behaviour","quote":"Before you start: steps 2.3 and 2.4 are done (HANDOFF State ticks them; the hosted ledger has the `9999_init.sql` row), the latest `main` CI run is green, and the Vercel team `Anacast` is on **Pro**","problem":"Days can pass between 2.4 and 2.5; a Free project pauses after 7 idle days and the first deployment would then fail every DB call, sending the founder into 2.6 for a non-bug. 4.5, 5.9 and 6.7 carry the 'project shows Active' precondition; 2.5 and 3.9 do not.","fix":"Add to the preconditions of 2.5 (and 3.9): 'the Supabase dashboard shows the project as Active — if it says Paused, click Restore and wait two minutes before you deploy'."},
{"step":"3.9","severity":"minor","category":"free-plan-behaviour","quote":"Before you start: steps 2.4, 2.5 and 3.8 are done (HANDOFF State), the latest `main` deployment on Vercel is green","problem":"Same as 2.5: M3 takes weeks of agent work during which nobody touches the hosted project; a paused project makes Part A/B fail at the first sign-in.","fix":"Add 'Supabase project shows Active (Restore if paused)' to the preconditions."},
{"step":"3.9","severity":"minor","category":"irreversible-action","quote":"When real churches pay: Stripe → switch to Live mode → recreate the product/price and the webhook endpoint in live mode → replace the three variables on Vercel Production only","problem":"Live mode also has its own Customer Portal configuration (item 4 is test-mode only) and its own Resend/sender settings are unaffected; without activating the portal in live mode `/api/billing/portal` fails for the first paying church. The go-live paragraph also omits that Stripe account activation (business details, bank account) takes days.","fix":"Extend item 10: 'complete Stripe account activation first (business details, bank account — allow a few days); in Live mode also Settings → Billing → Customer portal → Activate; then a real-card Checkout and refund'."},
{"step":"4.1a","severity":"major","category":"stale-decision","quote":"Android package `com.anacast.app` with the founder's `com.Anacast.app` recorded in a comment unless HANDOFF.md records a different confirmed value … the neutral `src/theme.ts` token file","problem":"Decision 18 confirmed the lowercase id with no case caveat and decision 17 replaced the neutral theme with the five-blue palette (mobile `theme.ts` is the single token file, also used for the splash and adaptive icon; 6.5 generates store assets from it). The 4.1a goal and done_when still ask for the comment and a neutral theme, so the Checkpoint C APK would not carry the brand and 6.5 would find no palette tokens.","fix":"Goal/Build/done_when: `android.package: 'com.anacast.app'` (decision 18, immutable after the first Play upload — no case comment); `src/theme/tokens.ts` holds the decision-17 palette with the confirmed roles (`ink` #00072d, `primaryDark` #001c55, `primary` #0a2472, `accent` #0e6ba8, `tint` #a6e1fa) and framework defaults for error/success/warning; `splash.backgroundColor` = ink, adaptive-icon background = primary. Remove 'neutral' from 4.6 and 4.5's 'if the founder confirmed com.Anacast.app' sentence."},
{"step":"2.1a","severity":"major","category":"stale-decision","quote":"`app/globals.css` holds the single `@theme` token block (neutral)","problem":"Decision 17 (and 32b) fix the web Tailwind tokens in 2.1a to the five-blue palette; 2.1a done_when and 3.7c ('Focus rings are visible on the neutral theme') still say neutral, so the Checkpoint A/B web app the founder trials would be unbranded and 3.7c's contrast audit would be redone later.","fix":"2.1a done_when/Build: the `@theme` block defines the decision-17 tokens (`--color-ink #00072d`, `--color-primary-dark #001c55`, `--color-primary #0a2472`, `--color-accent #0e6ba8`, `--color-tint #a6e1fa`) with the recorded contrast rules (no Icy on white text, Marine only for large text on Prussian); 3.7c audits that palette."},
{"step":"5.9","severity":"minor","category":"placeholder-clarity","quote":"`curl -s https://<DOMAIN>/.well-known/assetlinks.json`","problem":"Every earlier founder step uses `<VERCEL_URL>`; 5.9 switches to `<DOMAIN>` without saying it means the production origin recorded in HANDOFF (the Vercel URL until a custom domain exists — decision 31a), and it lacks the decision-31 sentence that a later domain change means re-verifying `assetlinks.json`, rebuilding the APK and updating the Supabase redirect list.","fix":"Add to the intro: '`<DOMAIN>` below is the production origin recorded in HANDOFF (`https://<VERCEL_URL>` until a custom domain is live). If the domain changes later, `assetlinks.json` must be re-verified, the APK rebuilt (`EXPO_PUBLIC_WEB_URL`), and the Supabase redirect list and store privacy URLs updated.'"},
{"step":"6.5","severity":"minor","category":"prerequisite-order","quote":"HANDOFF \"Applied migrations\" lists every file under `supabase/migrations/` as pasted (9999, and 9998/9997 if they exist","problem":"6.5 depends on 6.7, which also pastes `9996_account_archival.sql` (6.3a case B) and any 9995-or-lower file; the precondition names only 9998/9997, so a founder ticking it literally could ship a store build against a database missing the archival RPCs the Deactivate/Delete rows call.","fix":"Write: 'every file under `supabase/migrations/` has a dated ledger row (step 6.7 did this — 9998, 9997, 9996 when present, and any 9995-or-lower file)'."},
{"step":"6.4","severity":"minor","category":"prerequisite-order","quote":"HANDOFF \"Applied migrations\" lists every file under `supabase/migrations/` as pasted (9999, and 9998/9997 if they exist","problem":"Same gap as 6.5: 9996 (6.3a case B) and 9995-or-lower files are not named although 6.4 depends on 6.7 and the TestFlight checklist exercises Deactivate/Restore (item 19e).","fix":"Same sentence as proposed for 6.5, pointing at step 6.7."},
{"step":"6.6","severity":"minor","category":"dangling-reference","quote":"`Production domain: <DOMAIN>` (or `<VERCEL_URL>` if the custom domain from \"Still open for the founder\" item 2 is not live yet — see F)","problem":"HANDOFF's 'Still open for the founder' section now reads 'Nothing' (decisions 31–33); the domain status lives under Decisions / Environment status. The founder is sent to an item that no longer exists.","fix":"Replace with 'or `<VERCEL_URL>` while no custom domain is live (HANDOFF → Decisions: production domain deferred — decision 31a)'; same in the prompt's Inputs and HANDOFF reads."},
{"step":"NEW","severity":"major","category":"how-to-use","quote":"Founder answers of 2026-09-03 (recorded in `HANDOFF.md`) and drafting decisions of 2026-09-04 … App display name **Anacast**; Android package id `com.anacast.app` (founder wrote `com.Anacast.app`; lowercase recommended and immutable after the first Play upload; confirm in step 0.1); … brand colours and production domain deferred (neutral theme, `NEXT_PUBLIC_APP_URL`) … The 12-toggle permission mapping in `docs/RLS.md` ships as the default (not explicitly confirmed by the founder) … after that every change is a new `9998_*` file.","problem":"The 'Decisions these prompts assume' block in docs/TODO.md (written by assemble.py) is the first thing the founder reads and it contradicts HANDOFF on four settled points: package id confirmed (decision 18), five-blue palette with confirmed token roles (17/32b), permission mapping accepted (33), migration numbering with the reserved block (21); it also omits the account-archival/30-day-deletion decision (15), the privacy-policy step (16/19) and the 'no domain yet — Vercel URL' decision (31a), and the header dates stop at session 003.","fix":"In assemble.py rewrite the block: package id `com.anacast.app` confirmed 2026-09-07 (immutable after the first Play upload); palette Prussian/Deep Navy/Imperial/Bright Marine/Icy with roles ink/primaryDark/primary/accent/tint confirmed 2026-09-08; permission mapping accepted 2026-09-08; production domain deferred — Vercel URL via `NEXT_PUBLIC_APP_URL`/`EXPO_PUBLIC_WEB_URL` until the features work; Deactivate (reversible archive) vs Delete (30-day grace, Play requirement); placeholder privacy policy replaced by lawyer text in 6.6; `9999_init.sql` frozen after 2.4, then 9998/9997/9996 reserved for 5.6a/6.1a/6.3a and everything else from 9995 downward. Header: 'drafted in sessions 002–003, revised and decided in sessions 008–009'."},
{"step":"NEW","severity":"major","category":"how-to-use","quote":"4. After a step fails or an agent reports a deviation, use the reusable fix prompt (step 3.10) with the observations pasted in. Do not skip verification steps.","problem":"The founder is given one recovery path for every situation, but the list actually has several and 3.10 only exists after Checkpoint B: deployment/build/email/paused-project problems → 2.6; web bugs after Checkpoint B → 3.10; Android → 4.5's helper or 4.6; Phase 2 → 5.9's helper; store/TestFlight → the 6.4/6.5/6.8 second passes. Nothing says what to do when an agent step in M1–M2 ends with red checks (re-run the same prompt in a fresh session with 'Previous attempt: <final message>; the repo is at commit <hash>; continue, do not start over'), what 'reports a deviation' means (HANDOFF → 'Deviations from plan' — read it after every step and answer any question the agent left in its final message before starting the next step), or that a prompt may stop and ask a question that the founder must answer in that same session.","fix":"Replace item 4 with a short routing table (situation → prompt) and two rules: (a) if an agent ends with failing checks or an unfinished step, open a fresh session, paste the same prompt plus 'Previous attempt ended with: <paste final message>. Continue from the current state of `main`; do not start over.'; (b) after every step read HANDOFF 'Deviations from plan' and the agent's final message; a question there is yours to answer before the next step — decisions you veto go to the owning step's notes."},
{"step":"NEW","severity":"major","category":"how-to-use","quote":"5. Checkpoints: **A** (step 2.5) you are signed in on Vercel as super admin; **B** (step 3.9) the finished web app is trialled on Vercel; **C** (step 4.5) the Android preview build works. Everything after C completes Phases 2 and 3 of the plan.","problem":"Checkpoint D (5.9, Phase 2 trial incl. the 9998 paste) is missing; founder+agent steps (6.4, 6.5, 6.6, 6.8) run 'agent first pass → founder checklist → agent second pass (paste a mode line)' but the section describes only pure agent and pure founder steps; template prompts (2.6, 3.10, 4.6, the 4.5/5.9 helpers) have fenced fill-in blocks the founder must complete; founder steps also involve Firebase, Google Play and Apple, not only 'Supabase dashboard, Vercel, Stripe, Expo'; and the single most important safety rule — never paste a key, password or real email into a prompt because `prompt_history.txt` is committed verbatim — appears only inside individual checklists.","fix":"Add Checkpoint D (5.9) to item 5; add an item 'Founder + agent steps: run the agent prompt (first pass), follow the checklist, then paste the same prompt again with the mode line the prompt names (`review`, `release`, results) for the second pass'; add 'Some prompts are templates with a fenced block — fill every `<…>` before pasting'; list Firebase, Google Play Console and Apple Developer among the consoles; add a bold rule 'Never type a key, password, token or real email into a Claude Code prompt or chat — use the password manager; the URL is the only value you paste'."},
{"step":"NEW","severity":"major","category":"how-to-use","quote":"Add one line at the top if the session must work on a branch other than `main`. The agent ticks its own checkbox in this file, updates `HANDOFF.md`, commits and pushes.","problem":"docs/TODO.md and its kit live on `claude/project-status-todo-plan-leghlm` and decision 32c forbids merging until the founder says so; but every agent prompt says 'Work on `main`' and ticks its checkbox in `docs/TODO.md` — on `main`, where the file does not exist. The founder is never told that saying 'merge' (or naming the branch in every session) is the precondition for step 1.1.","fix":"Add item 0: 'Before the first agent step, tell a Claude Code session to merge `claude/project-status-todo-plan-leghlm` into `main` (fast-forward or merge commit, push) so `docs/TODO.md` and `HANDOFF.md` are on `main`; until then, start every session with the line \"Work on branch `claude/project-status-todo-plan-leghlm` for this session.\"' Record the merge date in HANDOFF."},
{"step":"4.5","severity":"minor","category":"stale-decision","quote":"if the founder confirmed `com.Anacast.app` in 0.1, that is the value to type","problem":"Decision 18 removed the case caveat; the notes still allow a mixed-case Firebase package name, and a mismatch between Firebase and `app.config.ts` would break every push at Checkpoint C.","fix":"Notes: 'The Firebase Android package name must be exactly `com.anacast.app` (decision 18) — print it with `npx expo config --type public | grep package` and compare letter by letter.'"}
]}
